// Targeted by JavaCPP version 1.5.4-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.libtorch;

import org.bytedeco.javacpp.Loader;
import org.bytedeco.javacpp.Pointer;
import org.bytedeco.javacpp.annotation.Namespace;
import org.bytedeco.javacpp.annotation.Properties;
 // namespace functional

// ============================================================================

/** Options for the {@code NLLLoss} module.
 * 
 *  Example:
 *  <pre>{@code
 *  NLLLoss model(NLLLossOptions().ignore_index(-100).reduction(torch::kMean));
 *  }</pre> */
@Namespace("torch::nn") @Properties(inherit = org.bytedeco.libtorch.presets.libtorch.class)
public class NLLLossOptions extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public NLLLossOptions() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public NLLLossOptions(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public NLLLossOptions(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public NLLLossOptions position(long position) {
        return (NLLLossOptions)super.position(position);
    }
    @Override public NLLLossOptions getPointer(long i) {
        return new NLLLossOptions(this).position(position + i);
    }


  /** A manual rescaling weight given to each
   *  class. If given, it has to be a Tensor of size {@code C}. Otherwise, it is
   *  treated as if having all ones. */
  /** Specifies a target value that is ignored
   *  and does not contribute to the input gradient. */
  /** Specifies the reduction to apply to the output. Default: Mean */
}
